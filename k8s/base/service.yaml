apiVersion: v1
kind: Service
metadata:
  name: image-classifier-service
  namespace: image-classifier
  labels:
    app: image-classifier
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: image-classifier
